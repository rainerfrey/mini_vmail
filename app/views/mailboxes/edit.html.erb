<% title t(:'resource.edit', :model => Mailbox.model_name.human) %>

<%= render 'form' %>

<p class="actions">
  <%= link_to t(:'actions.show'), @mailbox %> |
  <%= link_to t(:'title.mailboxes'), mailboxes_path %>
</p>
